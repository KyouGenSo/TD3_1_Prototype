{
    "type": "array",
    "items": {
        "type": "object",
        "required": ["name", "image", "description", "status-type", "operatorType", "value", "rarity", "event"],
        "additionalProperties": false,
        "properties": {
            "name": {
                "type": "string",
                "description": "アイテム名"
            },
            "image" : {
                "type": "string",
                "description": "アイテムの画像パス"
            },
            "description": {
                "type": "string",
                "description": "説明文"
            },
            "status-type": {
                "type": "string",
                "description": "ステータス名",
                "enum": ["heal", "health", "speed", "attack", "defense", "critical", "jump", "bullet_size"]
            },
            "operatorType": {
                "type": "string",
                "description": "効果を適用するときに使用する演算子",
                "enum": ["add", "sub", "mul", "div"]
            },
            "value": {
                "type": "number",
                "description": "効果の値"
            },
            "rarity": {
                "type": "integer",
                "description": "レアリティ",
                "minimum": 1,
                "maximum": 5
            },
            "event": {
                "type": "string",
                "description": "発動イベント。 onGetは取得時, onHitは攻撃時, onKillは敵を倒したときに発動, intervalは一定時間ごとに発動します。",
                "enum": ["onGet", "onHit", "onKill", "interval"]
            },
            "interval" : {
                "type": "number",
                "description": "効果発動の間隔。intervalイベントでのみ使用可",
                "default": 0
            }
        }
    }
}